# NATS
NATS_URL=nats://localhost:4222

# Kubernetes
KUBECONFIG=~/.kube/config
K8S_NAMESPACE=dejavu-apps

# Domain
BASE_DOMAIN=dejavu.local

# Database (for updating deployment status)
DB_HOST=localhost
DB_PORT=5432
DB_USER=dejavu
DB_PASSWORD=dejavu_secret
DB_NAME=dejavu
DB_SSLMODE=disable

