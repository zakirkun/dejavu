# DNSMasq configuration for local development
# Wildcard subdomain untuk *.dejavu.local

address=/.dejavu.local/127.0.0.1

# Optional: specify interface
# interface=lo0

# Don't read /etc/hosts
# no-hosts

# Log queries (untuk debugging)
# log-queries

